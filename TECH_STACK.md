# Camp-Web 前端技术栈文档

## 📋 技术栈概览

### 核心框架
- **Vue 3.4.15** - 渐进式 JavaScript 框架
  - 使用 Composition API（`<script setup>` 语法）
  - 响应式数据绑定
  - 组件化开发

### 构建工具
- **Vite 5.0.11** - 新一代前端构建工具
  - 快速热模块替换（HMR）
  - 基于 ES 模块的开发服务器
  - 优化的生产构建

### 路由管理
- **Vue Router 4.6.4** - 官方路由管理器
  - 支持 History API 路由模式
  - 路由守卫（beforeEach）实现登录验证
  - 嵌套路由结构
  - 路由懒加载

### HTTP 请求
- **原生 Fetch API** - 浏览器原生 API
  - 无第三方 HTTP 库依赖
  - 自定义请求封装（在 API 服务中）
  - 支持 JSON 数据格式

### 人脸识别
- **face-api.js 0.22.2** - 人脸识别库
  - 通过 CDN 引入
  - 支持实时人脸检测
  - 用于登录页面的人脸验证功能

### 状态管理
- **无状态管理库**（如 Pinia/Vuex）
  - 使用 `localStorage` 存储用户信息
  - 存储项：
    - `token` - 认证令牌
    - `userInfo` - 用户信息（JSON 字符串）

### UI 组件
- **无 UI 组件库**（如 Element Plus、Ant Design Vue）
  - 使用原生 HTML + CSS 构建
  - 自定义组件：
    - `LoginPage.vue` - 登录页面
    - `SidebarMenu.vue` - 侧边栏菜单
    - `Toast.vue` - 消息提示
    - `ActivatePasswordDialog.vue` - 密码激活对话框
    - `EditDescriptionDialog.vue` - 编辑描述对话框

### 样式方案
- **原生 CSS**
  - 全局样式文件：`src/style.css`
  - 组件级样式（`<style>` 标签）
  - 响应式设计支持

### 开发语言
- **JavaScript (ES6+)**
  - ES 模块（`import/export`）
  - 箭头函数、解构赋值、模板字符串
  - `async/await` 异步处理
  - 可选链操作符（`?.`）

### 环境配置
- **Vite 环境变量**
  - 使用 `import.meta.env` 访问环境变量
  - 配置项：
    - `VITE_API_BASE_URL` - API 基础地址（默认：`http://localhost:8080`）

### 项目结构
```
camp-web/
├── src/
│   ├── api/                    # API 服务层
│   │   ├── auth.js            # 认证相关 API
│   │   ├── student.js         # 学生管理 API
│   │   ├── score.js           # 成绩管理 API
│   │   ├── user.js            # 用户管理 API
│   │   ├── role.js            # 角色管理 API
│   │   ├── permission.js      # 权限管理 API
│   │   ├── announcement.js   # 公告管理 API
│   │   ├── visitor.js         # 来访登记 API
│   │   ├── attendance.js      # 考勤管理 API
│   │   ├── course.js          # 课程管理 API
│   │   ├── class.js           # 班级管理 API
│   │   ├── grade.js           # 年级管理 API
│   │   ├── examSchedule.js    # 考试安排 API
│   │   ├── classCourse.js     # 班级课程 API
│   │   ├── emergencyContact.js # 应急联系人 API
│   │   ├── dataPermission.js  # 数据权限 API
│   │   ├── teacherClass.js    # 教师班级关联 API
│   │   ├── gradeAdminGrade.js # 年级管理员关联 API
│   │   └── weather.js         # 天气 API
│   ├── components/             # 公共组件
│   │   ├── LoginPage.vue      # 登录页面组件
│   │   ├── SidebarMenu.vue    # 侧边栏菜单组件
│   │   ├── Toast.vue          # 消息提示组件
│   │   ├── ActivatePasswordDialog.vue  # 密码激活对话框
│   │   └── EditDescriptionDialog.vue   # 编辑描述对话框
│   ├── views/                  # 页面视图
│   │   ├── HomePage.vue        # 主页（包含侧边栏和内容区）
│   │   ├── HomeDashboard.vue   # 首页仪表盘
│   │   ├── StudentManagementPage.vue    # 学生信息管理
│   │   ├── ScoreManagementPage.vue      # 成绩管理
│   │   ├── UserManagementPage.vue      # 用户管理
│   │   ├── RoleManagementPage.vue      # 角色权限管理
│   │   ├── AnnouncementManagementPage.vue # 公告管理
│   │   ├── VisitorManagementPage.vue    # 来访登记管理
│   │   ├── AttendanceManagementPage.vue  # 考勤管理
│   │   ├── CourseManagementPage.vue     # 课程管理
│   │   ├── ClassManagementPage.vue       # 班级管理
│   │   ├── GradeManagementPage.vue        # 年级管理
│   │   ├── ExamScheduleManagementPage.vue # 考试安排管理
│   │   ├── ClassCourseManagementPage.vue  # 班级课程管理
│   │   ├── EmergencyContactManagementPage.vue # 应急联系人管理
│   │   ├── DataPermissionManagementPage.vue    # 数据权限管理
│   │   ├── TeacherClassManagementPage.vue     # 教师班级关联
│   │   ├── GradeAdminGradeManagementPage.vue  # 年级管理员关联
│   │   ├── ProfilePage.vue                    # 个人信息
│   │   ├── ChangePasswordPage.vue             # 修改密码
│   │   ├── ResetPasswordPage.vue              # 重置密码
│   │   └── PlaceholderPage.vue                # 占位页面
│   ├── config/                 # 配置文件
│   │   └── api.js             # API 配置（基础 URL、端点）
│   ├── router/                 # 路由配置
│   │   └── index.js           # 路由定义和守卫
│   ├── App.vue                # 根组件
│   ├── main.js                # 应用入口
│   └── style.css              # 全局样式
├── public/                    # 静态资源目录
├── index.html                 # HTML 模板
├── vite.config.js            # Vite 配置文件
├── package.json              # 项目依赖配置
└── README.md                 # 项目说明文档
```

## 🔧 开发工具和配置

### 开发服务器
- **端口**：3000
- **自动打开浏览器**：是
- **HMR**：支持热模块替换

### 构建配置
- **输出目录**：`dist`
- **资源目录**：`dist/assets`
- **生产环境优化**：自动启用

### 依赖管理
- **包管理器**：npm
- **Node.js 版本要求**：建议 16+ 或更高

## 📦 主要依赖

### 生产依赖（dependencies）
```json
{
  "vue": "^3.4.15",           // Vue 3 核心框架
  "vue-router": "^4.6.4"      // Vue Router 路由管理
}
```

### 开发依赖（devDependencies）
```json
{
  "@vitejs/plugin-vue": "^5.0.3",  // Vite Vue 插件
  "vite": "^5.0.11"                // Vite 构建工具
}
```

### CDN 依赖
- **face-api.js 0.22.2** - 通过 `<script>` 标签在 `index.html` 中引入

## 🎯 技术特点

### 1. 轻量级架构
- 无重型 UI 组件库，减少打包体积
- 使用原生 API，减少第三方依赖
- 自定义组件满足业务需求

### 2. 现代化开发体验
- Vite 提供极速的开发服务器启动
- Vue 3 Composition API 提供更好的代码组织
- ES 模块支持，代码更清晰

### 3. 路由管理
- 嵌套路由结构，支持多级页面
- 路由守卫实现登录验证
- 路由懒加载，优化首屏加载速度

### 4. API 服务层
- 统一的 API 请求封装
- 集中管理 API 端点配置
- 支持环境变量配置 API 地址

### 5. 状态持久化
- 使用 `localStorage` 存储用户认证信息
- 页面刷新后保持登录状态
- 简单的状态管理方案

## 🚀 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

## 📝 代码规范

### 组件命名
- 组件文件使用 PascalCase：`StudentManagementPage.vue`
- 组件内部使用 kebab-case：`<student-management-page>`

### API 服务
- API 文件使用 camelCase：`student.js`
- API 方法使用动词开头：`getStudentList()`, `createStudent()`

### 样式规范
- 使用原生 CSS，无预处理器
- 组件样式使用 scoped 或模块化类名
- 全局样式定义在 `style.css`

## 🔄 数据流

1. **用户操作** → Vue 组件
2. **组件调用** → API 服务层
3. **API 请求** → 后端服务（camp-app）
4. **响应数据** → API 服务层处理
5. **更新视图** → Vue 响应式更新

## 🎨 UI/UX 特点

- **响应式设计**：支持不同屏幕尺寸
- **原生样式**：无 UI 框架依赖，样式完全自定义
- **交互反馈**：Toast 消息提示、加载状态显示
- **表单验证**：前端基础验证 + 后端验证

## 🔐 安全特性

- **Token 认证**：JWT Token 存储在 localStorage
- **路由守卫**：未登录用户自动跳转登录页
- **请求头**：自动添加认证信息（在 API 层实现）

## 📈 性能优化

- **路由懒加载**：按需加载页面组件
- **Vite 构建优化**：生产环境自动优化
- **CDN 资源**：face-api.js 使用 CDN 加载

## 🔮 未来可能的技术升级

根据 README.md 中的规划，未来可能添加：
- [ ] Pinia 状态管理库
- [ ] 请求拦截器（axios 或 fetch 封装）
- [ ] Token 刷新机制
- [ ] 更完善的错误处理
- [ ] UI 组件库（如 Element Plus 或 Ant Design Vue）

## 📚 相关文档

- [项目 README](./README.md)
- [快速开始指南](./QUICK_START.md)
- [构建指南](./BUILD_GUIDE.md)

---

**最后更新**：2024年
**维护者**：Camp Web 开发团队
